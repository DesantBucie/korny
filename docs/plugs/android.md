Android supplies two functions: `_adb_rdy` and `_fastboot_rdy`, which print exactly their names,
if the phone is connected in one of those modes. If you don't have android platform tools, they won't print.
