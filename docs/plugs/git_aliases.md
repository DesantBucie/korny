Just look what aliases do. Some use _git_branch funtion to automatically fill branch

**ghcl()** clones github repository. You call it like that:

`$ ghcl DesantBucie/korny`

The gitlab counterpart is **glcl()**
