#!/usr/bin/env ksh
[[ $KORNY_FOLDER != "" ]] && $(cd $KORNY_FOLDER && git stash && git pull --rebase --stat origin master) 2&> /dev/null \
|| $(cd ~/.korny && git stash && git pull --rebase --stat origin master) 2&> /dev/null   

